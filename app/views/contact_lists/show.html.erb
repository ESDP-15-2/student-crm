<h1>Список "<%= @contact_list.title %>"</h1>

<h4>Включает в себя следущие контакты:</h4>

<table class="table table-striped table-hover">
  <tr>
    <th>Имя</th>
    <th>Номер телефона</th>
  </tr>
  <% @contact_list.students.each do |student| %>
      <tr>
        <td><%= link_to student.name, student_url(student) %></td>
        <td><%= student.phone %></td>
      </tr>
  <% end %>
</table>

<h5><%= link_to 'Назад', contact_lists_url %></h5>